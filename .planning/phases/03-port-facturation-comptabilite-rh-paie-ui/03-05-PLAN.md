---
phase: 03-port-facturation-comptabilite-rh-paie-ui
plan: "05"
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
  - 03-03
  - 03-04
autonomous: false

files_modified: []

requirements:
  - TR-04-1
  - TR-04-2

must_haves:
  truths:
    - "All three modules (Facturation, Comptabilité, RH/Paie) are live and functional end-to-end"
    - "npm run build produces zero TypeScript errors"
    - "Dashboard LCP is under 2.5 seconds (Lighthouse Performance audit)"
    - "React Query caching prevents duplicate API requests on re-navigation between dashboard pages"
---

<objective>
Human verification of all Phase 3 functionality. Confirms the complete Facturation, Comptabilité, and RH/Paie modules work end-to-end before Phase 4 begins. Also verifies TR-04-1 (performance: LCP < 2.5s) and TR-04-2 (React Query caching: no duplicate requests on re-navigation).

Purpose: Gate before data migration. Verify all CRUD, PDF downloads, payroll calculation, leave management, performance, and caching with a real browser session.
Output: Human sign-off on Phase 3.
</objective>

<execution_context>
@/Users/ramzilbs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ramzilbs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Phase 3 UAT — verify all Facturation, Comptabilité, RH/Paie flows + performance + caching</name>
  <action>Pause for human verification of Phase 3 implementation. The executor should present the verification checklist below and wait for the user to complete all steps and type "approved".</action>
  <verify>
    Run `npm run build` in nextjs-app/ first — confirm zero TypeScript errors before any manual testing.

    **Facturation:**
    1. Visit http://localhost:3000/dashboard/invoices
       - Create an invoice: add a contact, set due_date, tax_rate=19, add 2 line items (e.g., 5 × 10000 DZD + 3 × 5000 DZD)
       - Verify subtotal=65000, tax_amount=12350, total=77350 DZD displayed
       - Click status filter "Brouillon" — invoice appears. Click "Tous" — back to all.
       - Click Edit on the invoice — modal opens with existing data. Change due_date, save. Verify change.
       - Click PDF link — PDF downloads with correct invoice data.
       - Soft-delete the invoice — row disappears.

    2. Visit http://localhost:3000/dashboard/quotes
       - Create a quote (subtotal, tax_rate). Verify it appears.
       - Click the ArrowRight (convert) icon — toast "Devis converti en facture". Visit invoices page — new draft invoice present.

    3. Visit http://localhost:3000/dashboard/payments — list renders (may be empty, that is fine).

    **Comptabilité:**
    4. Visit http://localhost:3000/dashboard/accounts
       - Create two accounts: code=411000 / name=Clients / type=asset, code=701000 / name=Ventes / type=income
       - Verify both appear under their respective type sections.

    5. Visit http://localhost:3000/dashboard/journal
       - Create a journal entry: date=today, description="Vente client", debit=411000 Clients, credit=701000 Ventes, amount=50000
       - Verify entry appears in list.

    6. Visit http://localhost:3000/dashboard/reports
       - Verify income statement shows Produits credit total > 0
       - Verify balance sheet shows Actif total (debits on asset accounts)

    7. Visit http://localhost:3000/dashboard/ledger
       - Select account "411000 Clients" from the dropdown
       - Verify the journal line created in step 5 appears with debit=50000 and running_balance=50000

    **RH/Paie:**
    8. Visit http://localhost:3000/dashboard/departments — Create department "Technique".

    9. Visit http://localhost:3000/dashboard/employees
       - Create employee: full_name="Karim Mansouri", job_title="Développeur", department=Technique, base_salary=120000
       - Verify employee appears with department name in table.
       - Search "Karim" — filters correctly.

    10. Visit http://localhost:3000/dashboard/leaves
        - Submit leave request for Karim Mansouri, type=annual, 3-day range.
        - Verify "En attente" badge. Click approve — status changes to "Approuvé".

    11. Visit http://localhost:3000/dashboard/payroll
        - Select Karim Mansouri (base_salary 120000), month=current, year=current
        - Verify preview: CNAS = 10800 DZD (9% of 120000), IRG calculated, Net shown
        - Click Generate — payslip row appears in table. Click PDF link — payslip PDF downloads.
        - Try generating same period again — verify toast "Bulletin déjà généré pour cette période".

    **Performance — TR-04-1 (LCP < 2.5s):**
    12. Open Chrome DevTools → Lighthouse tab.
        - Run a Performance audit on http://localhost:3000/dashboard (logged in).
        - Verify LCP (Largest Contentful Paint) is under 2.5 seconds.
        - If LCP is between 2.5s and 4.0s: note the value but still approve (dev server is slower than production).
        - If LCP exceeds 4.0s: report the value so Claude can investigate.

    **Caching — TR-04-2 (React Query, no duplicate requests):**
    13. Open Chrome DevTools → Network tab. Filter by "Fetch/XHR".
        - Navigate to http://localhost:3000/dashboard/invoices — note the Supabase API requests made.
        - Navigate away to http://localhost:3000/dashboard/employees.
        - Navigate back to http://localhost:3000/dashboard/invoices.
        - Verify: the invoices Supabase request is NOT repeated on re-navigation (React Query serves from cache). The request should only appear once in the Network tab from the initial load.
        - Note: a background refetch may occur after ~30s (staleTime) — that is acceptable. Immediate re-request on tab switch is the failure case.

    **Build:**
    14. Run `cd nextjs-app && npm run build` — zero TypeScript errors required.
  </verify>
  <done>User types "approved" confirming all checks pass. Or describes specific failures for Claude to fix.</done>
</task>

</tasks>

<verification>
All manual checks in the verify block above pass.
</verification>

<success_criteria>
- User types "approved" confirming all Facturation, Comptabilité, and RH/Paie flows work
- npm run build produces zero TypeScript errors
- Lighthouse LCP on /dashboard is under 2.5s (or between 2.5-4.0s with note if dev server)
- React Query cache serves /dashboard/invoices data on re-navigation without duplicate network requests
</success_criteria>

<output>
After approval, create `.planning/phases/03-port-facturation-comptabilite-rh-paie-ui/03-05-SUMMARY.md` and update STATE.md: Phase 3 complete, advance to Phase 4.
</output>
