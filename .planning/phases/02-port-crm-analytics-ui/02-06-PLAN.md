---
phase: 02-port-crm-analytics-ui
plan: "06"
type: execute
wave: 4
depends_on:
  - "02-05"
files_modified: []
autonomous: false
requirements:
  - FR-02-1
  - FR-02-2
  - FR-02-3
  - FR-02-4
  - FR-02-5
  - FR-09-3

must_haves:
  truths:
    - "All CRM CRUD operations persist in Supabase (verified by human)"
    - "Kanban drag-and-drop updates stage_id in the database"
    - "Analytics page shows real counts from live data"
    - "No TypeScript build errors (npm run build passes)"
  artifacts: []
  key_links: []
---

<objective>
Human verification of all Phase 2 functionality. The user confirms that CRUD operations work end-to-end, the Kanban drag-and-drop updates the database, and the analytics dashboard shows live numbers.

Purpose: Acts as the quality gate for Phase 2. Catches any runtime issues that TypeScript checking missed.
Output: Confirmed working Phase 2 — ready to proceed to Phase 3.
</objective>

<execution_context>
@/Users/ramzilbs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ramzilbs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run build and apply migrations</name>
  <files></files>
  <action>
Run the Next.js production build to catch any TypeScript or compilation errors:
```bash
cd /Users/ramzilbs/Desktop/radiance_crm/ERP/nextjs-app
npm run build 2>&1 | tail -30
```

If build fails, fix TypeScript errors before proceeding to the checkpoint.

Also remind the user to apply the two new migrations in Supabase:
1. `002_seed_stages.sql` — seeds default pipeline stages
2. `003_add_priority_to_tasks.sql` — adds priority column to tasks

These can be applied via Supabase Dashboard (SQL editor) or by running:
```bash
cd /Users/ramzilbs/Desktop/radiance_crm/ERP/nextjs-app
npx supabase db push
```
  </action>
  <verify>
`npm run build` exits with code 0 (no errors)
  </verify>
  <done>`npm run build` completes successfully with no TypeScript errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of all Phase 2 CRM + Analytics flows</name>
  <what-built>
Complete Phase 2 CRM + Analytics UI:
- Contacts page: live list with FTS search, create modal, soft-delete
- Companies page: live list with FTS search, create modal, soft-delete
- Deals page: Kanban board with @dnd-kit drag-and-drop, create modal
- Tasks page: list with status toggle, priority badges, filter tabs, create modal
- Analytics page: 4 KPI cards (contacts/companies/deals/pipeline value), deals-by-stage bar chart, audit log table
  </what-built>
  <how-to-verify>
Run `cd /Users/ramzilbs/Desktop/radiance_crm/ERP/nextjs-app && npm run dev` then test each flow:

**FIRST: Apply migrations in Supabase SQL editor:**
1. Open Supabase Dashboard → SQL editor
2. Run contents of `nextjs-app/supabase/migrations/002_seed_stages.sql`
3. Run contents of `nextjs-app/supabase/migrations/003_add_priority_to_tasks.sql`

**Contacts (http://localhost:3000/dashboard/contacts):**
1. Page loads — table visible with column headers
2. Click "Nouveau contact" — modal opens with Prénom/Nom/Email/Téléphone fields
3. Fill form, click "Créer" — contact appears in list without page refresh
4. Type in search box — list filters by name
5. Click Trash icon on a contact — row disappears (soft delete)

**Companies (http://localhost:3000/dashboard/companies):**
1. Page loads with columns: Nom, Secteur, Site web, Créé le
2. Create a company via modal — appears in list
3. Delete a company — disappears from list

**Deals — Kanban (http://localhost:3000/dashboard/deals):**
1. Page shows 6 columns: Prospect, Qualifié, Proposition, Négociation, Gagné, Perdu
2. Create a deal via "Nouvelle opportunité" — card appears in correct stage column
3. Drag a deal card from one column to another — card moves, verify in Supabase dashboard that `stage_id` changed

**Tasks (http://localhost:3000/dashboard/tasks):**
1. Page loads with filter tabs: Toutes / En cours / Terminées
2. Create a task with Haute priority — appears with red priority badge
3. Click the status toggle on a task — switches between En cours and Terminé
4. Filter tab "En cours" shows only incomplete tasks
5. Filter tab "Terminées" shows only completed tasks

**Analytics (http://localhost:3000/dashboard/analytics):**
1. Page loads without console errors
2. 4 KPI cards show numbers (should match counts from Supabase)
3. Bar chart renders — shows deal counts by stage
4. Audit log section renders (may be empty if audit logging not yet triggered)

**Build check:**
Run `npm run build` — must exit with code 0, no TypeScript errors
  </how-to-verify>
  <files></files>
  <action>
Test all CRM and Analytics pages at http://localhost:3000 following the how-to-verify steps above.
Apply migrations 002_seed_stages.sql and 003_add_priority_to_tasks.sql in Supabase SQL editor before testing.
  </action>
  <verify>Human confirms all listed flows work correctly</verify>
  <done>User types "approved" — all CRUD, Kanban drag-and-drop, and analytics flows verified</done>
  <resume-signal>
Type "approved" if all flows work correctly.
If issues found, describe which page/feature failed and what the error was.
  </resume-signal>
</task>

</tasks>

<verification>
- `npm run build` exits 0
- All 5 pages tested by human: contacts, companies, deals, tasks, analytics
- Kanban drag-and-drop confirmed to update DB (stage_id changed in Supabase dashboard)
- No JavaScript console errors on any page
</verification>

<success_criteria>
- Human confirms "approved" after testing all 5 flows
- `npm run build` passes clean
- Phase 2 requirements FR-02-1 through FR-02-5 and FR-09-3 all verified working
</success_criteria>

<output>
After completion, create `.planning/phases/02-port-crm-analytics-ui/02-06-SUMMARY.md`

Also update `.planning/STATE.md`:
- Change Phase 2 status from "In progress" to "Complete"
- Update "Current plan" to "Phase 3 — needs planning"
- Add to Last Session section
</output>
